<% if !Favorite.exists?(user: current_user, task_id: task.id) %>
  <%= render 'favorites/favorite', task: task %>
<% else %>
  <%= render partial: 'favorites/unfavorite', locals: { task: task, favorite: @favorite } %>
<% end %>